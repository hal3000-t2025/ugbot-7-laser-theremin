# TODO

## 近期待办

- [x] 接入 `DFRobot DFR0789 LED Switch` 作为硬件音色切换按钮
- [x] 按当前建议接线：
  - `+` -> `ESP32-S3 3V3`
  - `-` -> `ESP32-S3 GND`
  - `D` -> `ESP32-S3 GPIO16`
- [x] 在固件中为 `GPIO16` 增加按钮输入
- [x] 实现“每按一次切到下一个音色”：
  - `sine`
  - `square`
  - `triangle`
  - `warm`
  - `hollow`
  - `bright`
- [x] 给按钮加入去抖
- [x] 把当前音色变化同步到串口 `status`
- [x] 把当前音高跨度从 `2 个八度` 扩到 `3 个八度`
  - 已从 `220Hz - 880Hz` 扩到 `220Hz - 1760Hz`
  - 仍需后续继续评估扩到 `3 个八度` 后的可演奏性和采样变形程度
- [x] 放宽最大音量上限
  - 默认最大音量已从 `0.08` 提升到 `0.14`
  - 已新增运行时命令：`set max volume <0.05-0.25>`
  - 已接入 `save / load / defaults / status`

## 采样音色路线

- [x] 落地方案 A：`单声道短循环采样音色`
  - 已用 `src/audio/正弦波测试.wav` 截取并转换出 `3s / mono / 16-bit PCM / 16kHz`
  - 已完成单个 sample bank 原型
  - 已支持循环播放
  - 已支持按 pitch 变速播放
  - 已接入现有按钮/预设切换链，作为第 `7` 个预设 `sample`
- [ ] 评估最小可行版本：`单周期 wavetable` 扩展更多音色
- [ ] 评估中间版本：`短采样 + 固定音高回放`
- [ ] 评估可演奏版本：`短采样 + 变速重采样`
- [ ] 如果要更自然：
  - 加 `线性插值`
  - 加 `loop`
  - 加 `ADSR` 包络
- [ ] 评估采样来源：
  - 内置到 Flash
  - 放到 LittleFS / SPIFFS
  - 放到 SD 卡
- [x] 明确 `sample` 预设的产品语义
  - `sample` 不是固定速度播放器
  - 当前语义固定为：按 `pitch` 连续变速重采样
  - 后续更换 sample 内容时，仍沿用这套“可演奏采样音色”逻辑
- [x] 明确 `sample` 内容更新策略
  - 后续允许频繁替换 sample 素材
  - 默认接受“重新编译 + 重新刷机”
  - 当前不为 sample 内容更新引入 `OTA`

## 乐谱 / 自动播放路线

- [x] 支持“预设乐曲自动播放”模式
  - 已增加自动播放槽位，当前为 `9 example` 和 `10 example2`
  - 切到对应预设后自动开始播
- [x] 引入 `ABC` 记谱法作为乐曲输入格式
- [x] 明确第一版只支持 `ABC` 的简化子集：
  - 单声部旋律
  - 基础音符
  - 休止符
  - 基本时值
  - 基本速度
  - 基本调号
- [x] 新增本地转换脚本：
  - `tools/abc_to_score.py`
  - 将 `ABC` 转成固件可直接播放的事件数组
- [x] 在固件中加入 `score player / sequencer`
- [x] 设计统一预设列表：
  - 合成音色
  - 采样音色
  - 自动播放乐曲
- [x] 支持按钮切到某个预设编号后自动进入对应模式
- [x] 串口 `status` 输出当前预设编号、名称和模式
- [ ] 扩展成多首乐曲和多文件自动汇总生成
- [ ] 支持停止/继续/单次播放而不循环
- [ ] 评估把 `display_name` 也接入串口状态与预设提示

## 手感优化

- [ ] 把后续工作重心收敛到“手感打磨”
  - 当前除手感问题外，主功能链路已基本定型
  - 优先调手势映射、可控性和演奏体验，不再扩主功能方向
- [x] 重新设计 `pitch` 手势映射曲线第一版
  - 已从“线性手势位置 -> 对数频率”升级为“`power/gamma` 弧线 -> 对数频率”
  - 已新增可保存参数：`pitch curve gamma`
  - 已新增串口命令：`set curve pitch <0.30-2.50>`
- [ ] 明确 `volume` 与 `pitch` 的手感分工
  - `volume` 已经是带响应曲线的幂函数映射
  - `pitch` 已进入第一版 `power/gamma` 弧线映射
  - 下一步主要优化 `pitch`，而不是推翻整个 `volume` 逻辑
- [x] 落地第一种候选曲线：
  - `power/gamma`
- [ ] 继续评估其余候选曲线：
  - `smoothstep`
  - 分段曲线
- [x] 让曲线参数可调，便于现场试手感
- [ ] 区分“音准可控”和“快速表演感”两种曲线取向

## OLED 部署 / 测试

- [ ] 接入 `0.91"` `SSD1306` `I2C` 屏幕
  - `GND -> GND`
  - `VCC -> 3V3`
  - `SCL -> GPIO12`
  - `SDA -> GPIO11`
  - 与 `volume` 传感器共用同一条 `I2C` 总线
- [ ] 确认屏幕地址
  - 当前代码默认地址为 `0x3C`
  - 如果模块实际地址为 `0x3D`，后续需同步改配置
- [ ] 部署要求
  - 接好屏幕后执行 `pio run -e esp32-s3-devkitc-1`
  - 再执行 `pio run -e esp32-s3-devkitc-1 -t upload`
  - 烧录后查看串口日志，确认是否出现 `OLED ready on shared I2C address 0x3C.`
- [ ] 硬件测试要求
  - 开机后屏幕顶部应显示当前预设，例如 `4/10 warm`
  - 第二条图显示 `PIT` 或 `SPD`
  - 第三条图显示 `VOL`
  - 普通演奏 / `sample` 模式下：
    - 挥动 `pitch` 手时，`PIT` 条应变化
    - 挥动 `volume` 手时，`VOL` 条应变化
  - `example / example2` 自动播放模式下：
    - 顶部应显示 `9/10 example` 或 `10/10 example2`
    - 第一条应从 `PIT` 切换成 `SPD`
    - 遮挡 `pitch` 传感器时，`SPD` 条应上升
    - 遮挡 `volume` 传感器时，`VOL` 条应上升
  - 未接屏时，设备应继续正常启动，不影响声音和传感器功能

## 2.0 开发创意收集

- [ ] 改成双声道音频输出
  - 目标：支持左右声道
  - 目标：预留 `3.5mm` 耳机孔输出
  - 方向：评估替换 `MAX98357A`，改用双声道 `I2S DAC / codec`
- [ ] 增加“音色亮暗”旋钮
  - 方向：增加一个电位器
  - 方向：软件里做亮/暗滤波或高低频 cut
- [ ] 增加 `3D` 打印“丑蛋”造型外壳
  - 方向：统一考虑主板、喇叭、传感器、按钮的安装结构
  - 方向：保留双手演奏时的手势空间

## 推荐顺序

1. 先做按钮切音色
2. 再扩 `wavetable`
3. 最后再做真正的 `sample playback`
